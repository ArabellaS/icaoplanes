<div class="row">
<% if @plane.photo.key? %>
    <div class="p-5 mb-4 bg-light rounded-3" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= cl_image_path @plane.photo.key %>');">
  <% else %>
  <div class="p-5 mb-4 bg-light rounded-3" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.dassault-aviation.com/w_2000,f_auto,q_auto,g_center,dpr_auto,c_fill/wp-auto-upload/1/files/2021/06/DA00026229_S-1.jpg);">
  <% end %>
    <div class="container-fluid py-5">
      <h1 class="display-5 fw-bold"><%= @plane.name %></h1>
      <p class="col-md-8 fs-4"><%= @plane.description %></p>
      <%= link_to "Book this plane !", new_plane_booking_path(@plane.id), class: "btn btn-primary btn-lg" %>
    </div>
  </div>
  <div class="row align-items-md-stretch mb-3">
    <div class="col-md-6">
      <div class="h-100 p-5 text-bg-dark rounded-3">
        <h2>Some features</h2>
        <p><i class="fa-solid fa-location-dot"></i> <%= @plane.location %></p>
        <p><i class="fas fa-couch"></i> <%= @plane.seats %> seats</p>
        <p><i class="fas fa-tachometer-alt"></i> <%= @plane.speed %> km/h</p>
        <p><i class="fas fa-plane"></i> Cat. <%= @plane.category %></p>
        <%= link_to "Book this plane !", new_plane_booking_path(@plane.id), class: "btn btn-outline-light" %>
      </div>
      <% if policy(@plane).edit? %>
        <%= link_to "Edit this plane", edit_plane_path(@plane) %> |
      <% end %>
      <% if policy(@plane).destroy? %>
        <%= link_to "Delete this plane", plane_path, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
      <% end %>
    </div>
    <div class="col-md-6">
      <div class="h-100 p-5 bg-light border rounded-3">
        <h2>I want to see others</h2>
        <p>Or, keep it light and add a border for some added definition to the boundaries of your content. Be sure to look under the hood at the source HTML here as we've adjusted the alignment and sizing of both column's content for equal-height.</p>
        <%= link_to "All planes", planes_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>
